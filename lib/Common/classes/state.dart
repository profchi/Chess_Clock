class AppState {
  double defaultTime;
  double increments;
  bool inFocus;
  
  AppState (double defaultTime, double increments) {
    this.defaultTime = defaultTime;
    this.increments = increments;
    this.inFocus = false;
  }
  
  
}